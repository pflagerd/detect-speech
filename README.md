# detect-speech

I have a collection of .wav files gathered from various voice recorders .

I want to detect whether they have speech in them, because many are just background noise.

I may have other formats such as .mp3 or .mp4 in the future.

I want to detect the exact time offset within the file where blocks of speech begin and end.
